{
  "name": "decorated-api",
  "version": "1.0.0",
  "description": "Express wrapper to create Node APIs using decorators/annotations (if you come from Java) and a very simple DI to allow easy access to Express' `req` and `res` inside any service.",
  "main": "index.js",
 
  "scripts": {
    "test": "npx jest --testMatch '**/src/**/*.spec.ts' --coverage",
    "prepare": "husky"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "husky": "^9.0.11",
    "jest": "^29.7.0",
    "lint-staged": "^15.2.2",
    "prettier": "^3.2.5",
    "ts-jest": "^29.1.2"
  },
  "lint-staged": {
    "*.ts": "prettier --write"
  }
}
